services:
  hardware-acceleration:
    # --------------------------------------------------------------------------
    # Intel OpenVINO
    # --------------------------------------------------------------------------

    # device_cgroup_rules:
    #   - "c 189:* rmw"
    # devices:
    #   - /dev/dri:/dev/dri
    # volumes:
    #   - /dev/bus/usb:/dev/bus/usb

    # --------------------------------------------------------------------------
    # Intel OpenVINO (WSL)
    # --------------------------------------------------------------------------

    # devices:
    #   - /dev/dri:/dev/dri
    #   - /dev/dxg:/dev/dxg
    # volumes:
    #   - /dev/bus/usb:/dev/bus/usb
    #   - /usr/lib/wsl:/usr/lib/wsl

    # --------------------------------------------------------------------------
    # NVIDIA CUDA
    # --------------------------------------------------------------------------

    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           capabilities: [gpu]
    #           count: 1

    # --------------------------------------------------------------------------
    # Rockchip MPP
    # --------------------------------------------------------------------------
    
    # group_add:
    #   - video
    # devices:
    #   - /dev/dri:/dev/dri
    #   - /dev/kfd:/dev/kfd
